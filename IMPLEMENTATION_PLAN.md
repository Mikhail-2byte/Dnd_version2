# План реализации D&D Virtual Table - Прототип

## Статус проекта: MVP Реализован, требуется доработка и тестирование

**Дата последнего обновления:** 2024

**Архитектура:** Монолит (Python FastAPI + React)

**Цель:** Создать работающий прототип браузерного приложения для игры в D&D онлайн с базовой функциональностью: регистрация/логин, создание лобби, карта с токенами, синхронизация в реальном времени.

---

## 🚀 Быстрый старт для нейросети

**Если вы получили задачу на реализацию:**

1. **Перейдите к разделу [Пошаговый план реализации](#-пошаговый-план-реализации)** - там детальные инструкции для каждой задачи
2. **Найдите нужную задачу** по номеру (например, Задача 1.1, Задача 2.3)
3. **Следуйте пошаговым инструкциям** - каждая задача содержит:
   - Цель и контекст
   - Детальные шаги выполнения
   - Конкретные файлы и строки кода
   - Примеры кода (где необходимо)
   - Критерии готовности для проверки
4. **Проверьте критерии готовности** после выполнения
5. **Переходите к следующей задаче** только после завершения текущей

**Порядок выполнения:**
- **Фаза 1** (критично) → **Фаза 2** (тесты backend) → **Фаза 3** (тесты frontend) → **Фаза 4** (улучшения)

---

## 📋 Оглавление

1. [Что уже реализовано](#что-уже-реализовано)
2. [Пошаговый план реализации](#пошаговый-план-реализации) ⭐ **НАЧНИТЕ ОТСЮДА**
3. [Что нужно сделать](#что-нужно-сделать)
4. [Тестирование](#тестирование)
5. [Дополнительные улучшения](#дополнительные-улучшения)
6. [Порядок выполнения задач](#порядок-выполнения-задач)
7. [Известные проблемы и ограничения](#известные-проблемы-и-ограничения)
8. [Технические детали](#технические-детали-для-продолжения-разработки)
9. [Деплой](#деплой-для-будущего)

---

## ✅ Что уже реализовано

### Backend (Python FastAPI)

#### 1. Базовая структура проекта
- ✅ Структура папок организована по MVC паттерну
- ✅ Конфигурация через Pydantic Settings (`app/config.py`)
- ✅ Подключение к PostgreSQL через SQLAlchemy (`app/database.py`)
- ✅ Подключение к Redis (`app/redis_client.py`)
- ✅ Настройка CORS для фронтенда
- ✅ Статические файлы для загрузки карт (`/uploads`)

**Файлы:**
- `backend/app/__init__.py`
- `backend/app/config.py`
- `backend/app/database.py`
- `backend/app/redis_client.py`
- `backend/app/main.py`

#### 2. Модели базы данных (SQLAlchemy)
- ✅ Модель `User` - пользователи системы
  - Поля: id (UUID), email (unique), username, password_hash, created_at
  - Файл: `backend/app/models/user.py`
  
- ✅ Модель `GameSession` - игровые сессии (лобби)
  - Поля: id (UUID), name, invite_code (unique, 6 символов), master_id, map_url, created_at
  - Связи: master (User), participants, tokens
  - Файл: `backend/app/models/game_session.py`
  
- ✅ Модель `GameParticipant` - участники игр
  - Поля: game_id, user_id, role ('master' | 'player'), joined_at
  - Составной первичный ключ (game_id, user_id)
  - Файл: `backend/app/models/game_participant.py`
  
- ✅ Модель `Token` - токены на карте
  - Поля: id (UUID), game_id, name, x (float 0-100%), y (float 0-100%), image_url, character_id, created_at
  - Файл: `backend/app/models/token.py`

**Файлы:**
- `backend/app/models/__init__.py` - экспорт всех моделей
- `backend/app/models/user.py`
- `backend/app/models/game_session.py`
- `backend/app/models/game_participant.py`
- `backend/app/models/token.py`

#### 3. Миграции базы данных
- ✅ Настроен Alembic для миграций
- ✅ Конфигурация Alembic (`backend/alembic.ini`)
- ✅ Настройка окружения для миграций (`backend/alembic/env.py`)
- ⚠️ **ТРЕБУЕТСЯ**: Создать первую миграцию и применить её

**Файлы:**
- `backend/alembic.ini`
- `backend/alembic/env.py`
- `backend/alembic/script.py.mako`
- `backend/alembic/versions/.gitkeep`

#### 4. Pydantic схемы (валидация данных)
- ✅ `UserRegister` - регистрация пользователя
- ✅ `UserLogin` - вход в систему
- ✅ `UserResponse` - ответ с данными пользователя
- ✅ `TokenResponse` - ответ с JWT токеном
- ✅ `GameCreate` - создание игры
- ✅ `GameResponse` - ответ с данными игры
- ✅ `GameJoin` - присоединение к игре
- ✅ `TokenCreate` - создание токена
- ✅ `TokenUpdate` - обновление позиции токена
- ✅ `TokenResponse` - ответ с данными токена

**Файлы:**
- `backend/app/schemas/__init__.py`
- `backend/app/schemas/user.py`
- `backend/app/schemas/game.py`
- `backend/app/schemas/token.py`

#### 5. Утилиты
- ✅ `utils/security.py` - хеширование паролей (bcrypt)
  - `verify_password()` - проверка пароля
  - `get_password_hash()` - хеширование пароля
  
- ✅ `utils/jwt.py` - работа с JWT токенами
  - `create_access_token()` - создание токена
  - `decode_access_token()` - декодирование токена

**Файлы:**
- `backend/app/utils/__init__.py`
- `backend/app/utils/security.py`
- `backend/app/utils/jwt.py`

#### 6. Middleware
- ✅ `middleware/auth.py` - проверка JWT токенов
  - `get_current_user()` - получение текущего пользователя из токена
  - Использует HTTPBearer для извлечения токена
  - Валидация токена и загрузка пользователя из БД

**Файлы:**
- `backend/app/middleware/__init__.py`
- `backend/app/middleware/auth.py`

#### 7. Сервисы (бизнес-логика)
- ✅ `services/auth_service.py` - аутентификация
  - `register_user()` - регистрация нового пользователя
  - `authenticate_user()` - проверка логина/пароля
  - `create_user_token()` - создание JWT токена
  
- ✅ `services/game_service.py` - управление играми
  - `generate_invite_code()` - генерация уникального invite-кода (6 символов)
  - `create_game()` - создание игровой сессии
  - `get_game_by_invite_code()` - получение игры по invite-коду
  - `get_game_by_id()` - получение игры по ID
  - `join_game()` - присоединение к игре
  - `is_master()` - проверка прав мастера
  - `create_token()` - создание токена на карте
  - `update_token_position()` - обновление позиции токена
  - `delete_token()` - удаление токена
  - `get_game_tokens()` - получение всех токенов игры

**Файлы:**
- `backend/app/services/__init__.py`
- `backend/app/services/auth_service.py`
- `backend/app/services/game_service.py`

#### 8. API Endpoints (REST)
- ✅ `api/auth.py` - аутентификация
  - `POST /api/auth/register` - регистрация
  - `POST /api/auth/login` - вход
  - `GET /api/auth/me` - текущий пользователь (требует токен)
  
- ✅ `api/games.py` - управление играми
  - `POST /api/games` - создать лобби (требует токен)
  - `GET /api/games/{invite_code}` - информация о лобби
  - `POST /api/games/{game_id}/join` - присоединиться (требует токен)
  - `GET /api/games/{game_id}/info` - детали игры (требует токен)
  - `GET /api/games/{game_id}/tokens` - получить все токены (требует токен)
  - `POST /api/games/{game_id}/tokens` - создать токен (только мастер)
  - `PUT /api/games/{game_id}/tokens/{token_id}` - обновить позицию (только мастер)
  - `DELETE /api/games/{game_id}/tokens/{token_id}` - удалить токен (только мастер)
  
- ✅ `api/maps.py` - загрузка карт
  - `POST /api/maps/upload?game_id={game_id}` - загрузить карту (только мастер)
  - ⚠️ **ПРОБЛЕМА**: В коде `game_id` передается как обычный параметр, но для загрузки файлов нужен Query параметр
  - Нужно исправить: `game_id: UUID = Query(...)`
  - Валидация размера файла (макс 10MB)
  - Валидация типа файла (.jpg, .jpeg, .png, .webp)
  - Сохранение в `uploads/maps/`

**Файлы:**
- `backend/app/api/__init__.py`
- `backend/app/api/auth.py`
- `backend/app/api/games.py`
- `backend/app/api/maps.py`

#### 9. WebSocket (Socket.IO)
- ✅ Интеграция python-socketio с FastAPI
- ✅ Аутентификация через JWT токен при подключении
- ✅ Обработчики событий в `sockets/game_events.py`:
  - `connect` - подключение с проверкой токена
  - `disconnect` - отключение и очистка комнат
  - `game:join` - подключение к игре
  - `token:move` - перемещение токена (только мастер)
  - `token:create` - создание токена (только мастер)
  - `token:delete` - удаление токена (только мастер)
  
- ✅ События от сервера (broadcast):
  - `game:state` - полное состояние игры при подключении
  - `token:moved` - токен перемещен
  - `token:created` - токен создан
  - `token:deleted` - токен удален
  - `player:joined` - игрок подключился
  - `player:left` - игрок отключился
  - `error` - ошибка
  
- ✅ Хранение состояния в Redis:
  - `game:{game_id}:tokens` - Hash с токенами
  - TTL 24 часа для неактивных игр
  - Функции `save_game_state_to_redis()` и `load_game_state_from_redis()`

**Файлы:**
- `backend/app/sockets/__init__.py`
- `backend/app/sockets/game_events.py`

#### 10. Главный файл приложения
- ✅ `main.py` - точка входа FastAPI
  - Настройка CORS
  - Подключение роутеров
  - Интеграция Socket.IO
  - Статические файлы для карт

**Файлы:**
- `backend/app/main.py`
- `backend/run.py` - скрипт для запуска

#### 11. Зависимости
- ✅ `requirements.txt` со всеми необходимыми пакетами
- ✅ Версии зафиксированы

**Файлы:**
- `backend/requirements.txt`

---

### Frontend (React + TypeScript)

#### 1. Базовая структура проекта
- ✅ Настроен Vite с React 19 и TypeScript
- ✅ Настроен Tailwind CSS
- ✅ Настроен React Router для маршрутизации
- ✅ Конфигурация прокси для API и WebSocket

**Файлы:**
- `frontend/vite.config.ts`
- `frontend/tsconfig.json`
- `frontend/tailwind.config.js`
- `frontend/postcss.config.js`
- `frontend/index.html`
- `frontend/package.json`

#### 2. TypeScript типы
- ✅ `types/user.ts` - типы для пользователей
  - `User`, `TokenResponse`
  
- ✅ `types/game.ts` - типы для игр
  - `GameSession`, `Token`, `Player`

**Файлы:**
- `frontend/src/types/user.ts`
- `frontend/src/types/game.ts`

#### 3. Сервисы
- ✅ `services/api.ts` - REST API клиент
  - Настроен axios с базовым URL
  - Interceptor для добавления JWT токена
  - Interceptor для обработки ошибок (редирект на логин при 401)
  - `authAPI` - методы для аутентификации
  - `gamesAPI` - методы для работы с играми
  
- ✅ `services/socket.ts` - WebSocket клиент
  - Класс `SocketService` для управления подключением
  - Методы для подключения/отключения
  - Методы для отправки событий (join, move, create, delete)
  - Методы для подписки на события (onGameState, onTokenMoved, etc.)

**Файлы:**
- `frontend/src/services/api.ts`
- `frontend/src/services/socket.ts`

#### 4. Управление состоянием (Zustand)
- ✅ `store/authStore.ts` - состояние аутентификации
  - `user`, `token`, `isLoading`
  - `login()`, `register()`, `logout()`, `checkAuth()`
  - Сохранение токена в localStorage
  
- ✅ `store/gameStore.ts` - состояние игры
  - `currentGame`, `tokens`, `players`, `currentUserRole`
  - `setGame()`, `setTokens()`, `setPlayers()`, `setCurrentUserRole()`
  - `updateTokenPosition()`, `addToken()`, `removeToken()`, `reset()`

**Файлы:**
- `frontend/src/store/authStore.ts`
- `frontend/src/store/gameStore.ts`

#### 5. Страницы
- ✅ `pages/Login.tsx` - страница входа
  - Форма с email и password
  - Обработка ошибок
  - Редирект на регистрацию
  
- ✅ `pages/Register.tsx` - страница регистрации
  - Форма с email, username, password
  - Обработка ошибок
  - Редирект на вход
  
- ✅ `pages/MainMenu.tsx` - главное меню
  - Приветствие пользователя
  - Кнопка создания игры
  - Форма подключения по invite-коду
  - Кнопка выхода
  
- ✅ `pages/GameRoom.tsx` - страница игры
  - Загрузка данных игры
  - Подключение к WebSocket
  - Отображение GameMap и PlayerList
  - Обработка всех WebSocket событий
  - Кнопка выхода из игры

**Файлы:**
- `frontend/src/pages/Login.tsx`
- `frontend/src/pages/Register.tsx`
- `frontend/src/pages/MainMenu.tsx`
- `frontend/src/pages/GameRoom.tsx`

#### 6. Компоненты
- ✅ `components/CreateGameModal.tsx` - модалка создания игры
  - Форма с названием игры
  - Кнопки отмены и создания
  
- ✅ `components/JoinGameForm.tsx` - форма подключения
  - Поле ввода invite-кода (макс 6 символов, uppercase)
  - Кнопка присоединения
  
- ✅ `components/GameMap.tsx` - игровое поле
  - Отображение карты (из URL или дефолтная)
  - Отображение сетки
  - Отображение токенов
  - Передача событий перемещения
  
- ✅ `components/Token.tsx` - токен на карте
  - Drag & Drop функциональность
  - Визуальное отображение (круг с первой буквой имени)
  - Подсказка с именем при наведении
  - Ограничение перемещения только для мастера
  
- ✅ `components/PlayerList.tsx` - список игроков
  - Отображение всех участников
  - Индикатор роли (Мастер/Игрок)

**Файлы:**
- `frontend/src/components/CreateGameModal.tsx`
- `frontend/src/components/JoinGameForm.tsx`
- `frontend/src/components/GameMap.tsx`
- `frontend/src/components/Token.tsx`
- `frontend/src/components/PlayerList.tsx`

#### 7. Главный файл приложения
- ✅ `App.tsx` - корневой компонент
  - Настройка роутинга
  - Защищенные маршруты через `ProtectedRoute`
  - Автоматическая проверка авторизации

**Файлы:**
- `frontend/src/App.tsx`
- `frontend/src/main.tsx`
- `frontend/src/index.css`

---

### Инфраструктура

#### 1. Docker
- ✅ `docker-compose.yml` - конфигурация для PostgreSQL и Redis
  - PostgreSQL 15 на порту 5432
  - Redis 7 на порту 6379
  - Health checks настроены
  - Volumes для персистентности данных

**Файлы:**
- `docker-compose.yml`

#### 2. Конфигурация
- ⚠️ `.env.example` для backend (файл не создан, нужно создать из шаблона)
- ✅ `.env.example` для frontend (создан)
- ✅ `.gitignore` настроен правильно
- ❌ **Нет скрипта для генерации SECRET_KEY**

**Файлы:**
- `backend/.env.example` (нужно создать)
- `frontend/.env.example` (создан)
- `.gitignore` (создан)

**Требуется:**
- Создать `backend/.env.example` с правильными значениями
- Добавить скрипт `backend/generate_secret_key.py` для генерации безопасного SECRET_KEY

#### 3. Документация
- ✅ `README.md` - основная документация
- ✅ `SETUP.md` - инструкция по установке
- ✅ `MICROSERVICES_REFERENCE.md` - справочник по микросервисам

**Файлы:**
- `README.md`
- `SETUP.md`
- `MICROSERVICES_REFERENCE.md`

---

## 🎯 Пошаговый план реализации

> **ВАЖНО:** Этот раздел содержит детальные инструкции для нейросети по реализации задач. Каждая задача разбита на конкретные шаги с указанием файлов, строк кода и ожидаемых результатов.

### Фаза 1: Критические исправления (ОБЯЗАТЕЛЬНО ПЕРВЫМ)

#### Задача 1.1: Создание миграций базы данных

**Цель:** Создать и применить первую миграцию Alembic для создания всех таблиц в БД.

**Шаги выполнения:**

1. **Проверить настройки Alembic:**
   - Файл: `backend/alembic.ini` - должен содержать `sqlalchemy.url`
   - Файл: `backend/alembic/env.py` - должен импортировать модели и Base

2. **Создать миграцию:**
   ```bash
   cd backend
   alembic revision --autogenerate -m "Initial migration"
   ```
   - Проверить созданный файл в `backend/alembic/versions/`
   - Убедиться, что все модели (User, GameSession, GameParticipant, Token) включены

3. **Проверить миграцию:**
   - Открыть созданный файл миграции
   - Проверить, что создаются все таблицы:
     - `users` (id, email, username, password_hash, created_at)
     - `game_sessions` (id, name, invite_code, master_id, map_url, created_at)
     - `game_participants` (game_id, user_id, role, joined_at)
     - `tokens` (id, game_id, name, x, y, image_url, character_id, created_at)
   - Проверить индексы и внешние ключи

4. **Применить миграцию:**
   ```bash
   alembic upgrade head
   ```

5. **Проверить результат:**
   - Подключиться к PostgreSQL: `docker exec -it <postgres_container> psql -U dnd_user -d dnd_db`
   - Выполнить: `\dt` - должны быть видны все таблицы
   - Выполнить: `\d users` - проверить структуру таблицы

**Критерии готовности:**
- ✅ Миграция создана и находится в `backend/alembic/versions/`
- ✅ Миграция применена без ошибок
- ✅ Все 4 таблицы созданы в БД
- ✅ Индексы и внешние ключи созданы

**Файлы для изменения:**
- `backend/alembic/versions/XXXXXX_initial_migration.py` (создается автоматически)

---

#### Задача 1.2: Создание файлов окружения

**Цель:** Создать `.env` файлы для backend и frontend с правильными настройками.

**Шаги выполнения:**

1. **Создать `backend/.env.example` (если не существует):**
   ```env
   # Database
   DATABASE_URL=postgresql://dnd_user:dnd_password@localhost:5432/dnd_db
   
   # Redis
   REDIS_URL=redis://localhost:6379
   
   # JWT
   SECRET_KEY=your-secret-key-here-change-in-production
   ALGORITHM=HS256
   ACCESS_TOKEN_EXPIRE_MINUTES=30
   
   # Server
   HOST=0.0.0.0
   PORT=8000
   
   # Uploads
   UPLOAD_DIR=uploads
   MAPS_DIR=uploads/maps
   MAX_UPLOAD_SIZE=10485760
   
   # Logging
   LOG_LEVEL=INFO
   ```

2. **Создать скрипт для генерации SECRET_KEY:**
   - Файл: `backend/generate_secret_key.py`
   ```python
   import secrets
   print(secrets.token_urlsafe(32))
   ```
   - Выполнить: `python generate_secret_key.py` и скопировать результат

3. **Создать `backend/.env` из `.env.example`:**
   ```bash
   cd backend
   cp .env.example .env
   # Windows: copy .env.example .env
   ```
   - Заменить `SECRET_KEY` на сгенерированное значение
   - Проверить остальные значения (должны совпадать с docker-compose.yml)

4. **Проверить `frontend/.env.example` (должен существовать):**
   ```env
   VITE_API_URL=http://localhost:8000
   VITE_WS_URL=http://localhost:8000
   ```

5. **Создать `frontend/.env` из `.env.example`:**
   ```bash
   cd frontend
   cp .env.example .env
   # Windows: copy .env.example .env
   ```

**Критерии готовности:**
- ✅ `backend/.env.example` создан
- ✅ `backend/generate_secret_key.py` создан
- ✅ `backend/.env` создан с правильным SECRET_KEY
- ✅ `frontend/.env` создан
- ✅ Все переменные окружения соответствуют docker-compose.yml

**Файлы для создания:**
- `backend/.env.example`
- `backend/generate_secret_key.py`
- `backend/.env` (не коммитить в git!)
- `frontend/.env` (не коммитить в git!)

---

#### Задача 1.3: Исправление бага в `main.py` (экспорт socket_app)

**Проблема:** В конце файла `backend/app/main.py` экспортируется `socket_app` вместо `app`, что может вызвать проблемы при запуске.

**Текущий код (строки 57-59):**
```python
# Экспортируем socket_app для запуска через uvicorn
app = socket_app
```

**Решение:** Проверить, как запускается приложение. Если через `uvicorn app.main:app`, то нужно либо:
- Вариант 1: Экспортировать `socket_app` как `app`
- Вариант 2: Изменить команду запуска на `uvicorn app.main:socket_app`

**Шаги выполнения:**

1. **Проверить файл `backend/run.py`:**
   - Открыть `backend/run.py`
   - Посмотреть, как запускается приложение

2. **Выбрать решение:**
   - Если `run.py` использует `app.main:app`, оставить текущий код (он правильный)
   - Если `run.py` использует `app.main:socket_app`, изменить на `app.main:app` и убрать строку `app = socket_app`

3. **Проверить работу:**
   - Запустить: `python run.py` или `uvicorn app.main:app --reload`
   - Проверить, что сервер запускается без ошибок
   - Проверить, что WebSocket работает (подключиться через браузер)

**Критерии готовности:**
- ✅ Приложение запускается без ошибок
- ✅ REST API работает (проверить `/health`)
- ✅ WebSocket работает (проверить подключение)

**Файлы для изменения:**
- `backend/app/main.py` (строки 57-59)
- `backend/run.py` (если нужно)

---

#### Задача 1.4: Исправление бага в `GameRoom.tsx` (зависимости useEffect)

**Проблема:** В `frontend/src/pages/GameRoom.tsx` на строке 55 используется `game` в замыкании, но он не в зависимостях `useEffect`.

**Текущий код (строки 28-95):**
```typescript
useEffect(() => {
  // ...
  const loadGame = async () => {
    // ...
    const game = await gamesAPI.getInfo(gameId);
    setGame(game);
    // ...
    socketService.onGameState((state: GameState) => {
      // ...
      } else if (state.game.id && game.master_id === user?.id) {  // ← ПРОБЛЕМА: game используется здесь
        setCurrentUserRole('master');
      }
    });
  };
  loadGame();
}, [gameId, token, user?.id]);  // ← game отсутствует в зависимостях
```

**Решение:** Использовать `currentGame` из store вместо локальной переменной `game`.

**Шаги выполнения:**

1. **Открыть файл:** `frontend/src/pages/GameRoom.tsx`

2. **Исправить строку 55:**
   - Заменить `game.master_id` на `currentGame?.master_id`
   - Или использовать `state.game.master_id` (уже есть в state)

3. **Проверить зависимости:**
   - Убедиться, что `currentGame` не нужен в зависимостях (он из store)
   - Или добавить `currentGame` в зависимости, если используется

**Исправленный код:**
```typescript
} else if (state.game.id && state.game.master_id === user?.id) {
  setCurrentUserRole('master');
}
```

**Критерии готовности:**
- ✅ Код исправлен, нет предупреждений ESLint о зависимостях
- ✅ Приложение компилируется без ошибок
- ✅ Роль пользователя определяется правильно

**Файлы для изменения:**
- `frontend/src/pages/GameRoom.tsx` (строка 55)

---

#### Задача 1.5: Исправление бага в `maps.py` (game_id как Query параметр)

**Проблема:** В `backend/app/api/maps.py` параметр `game_id` должен быть Query параметром для загрузки файлов.

**Текущий код (строки 15-21):**
```python
@router.post("/upload")
async def upload_map(
    game_id: UUID,  # ← ПРОБЛЕМА: должен быть Query параметром
    file: UploadFile = File(...),
    ...
):
```

**Решение:** Изменить `game_id: UUID` на `game_id: UUID = Query(...)`

**Шаги выполнения:**

1. **Открыть файл:** `backend/app/api/maps.py`

2. **Добавить импорт Query:**
   - В строке 1 добавить `Query` в импорт из `fastapi`

3. **Изменить параметр:**
   - Заменить `game_id: UUID,` на `game_id: UUID = Query(...),`

**Исправленный код:**
```python
from fastapi import APIRouter, Depends, HTTPException, status, UploadFile, File, Query

@router.post("/upload")
async def upload_map(
    game_id: UUID = Query(...),
    file: UploadFile = File(...),
    ...
):
```

**Критерии готовности:**
- ✅ Код исправлен
- ✅ API endpoint работает (проверить через Swagger UI)
- ✅ Можно загрузить карту с параметром `?game_id=...`

**Файлы для изменения:**
- `backend/app/api/maps.py` (строки 1 и 17)

---

#### Задача 1.6: Добавление дефолтной карты

**Цель:** Добавить дефолтную карту для отображения, когда карта не загружена.

**Шаги выполнения:**

1. **Создать папку для изображений:**
   ```bash
   mkdir -p frontend/public/images
   # Windows: mkdir frontend\public\images
   ```

2. **Добавить дефолтную карту:**
   - Вариант 1: Использовать карту из `Example/dungeons-and-dragons-website/public/images/dnd-map.jpg`
   - Вариант 2: Создать простую placeholder карту
   - Скопировать файл в `frontend/public/images/default-map.jpg`

3. **Проверить компонент GameMap:**
   - Открыть `frontend/src/components/GameMap.tsx`
   - Убедиться, что используется путь `/images/default-map.jpg` для дефолтной карты
   - Если нет - добавить логику: `const mapUrl = game?.map_url || '/images/default-map.jpg'`

4. **Проверить работу:**
   - Запустить frontend
   - Создать игру без загрузки карты
   - Проверить, что отображается дефолтная карта

**Критерии готовности:**
- ✅ Файл `frontend/public/images/default-map.jpg` существует
- ✅ Компонент GameMap использует дефолтную карту, если карта не загружена
- ✅ Дефолтная карта отображается в браузере

**Файлы для создания/изменения:**
- `frontend/public/images/default-map.jpg` (создать)
- `frontend/src/components/GameMap.tsx` (проверить/изменить)

---

#### Задача 1.7: Добавление обработки переподключения WebSocket

**Цель:** Обеспечить автоматическое переподключение WebSocket при обрыве связи.

**Шаги выполнения:**

1. **Проверить настройки Socket.IO клиента:**
   - Открыть `frontend/src/services/socket.ts`
   - Проверить, есть ли настройки переподключения в `io()`:
     ```typescript
     const socket = io(url, {
       reconnection: true,
       reconnectionDelay: 1000,
       reconnectionAttempts: 5,
       reconnectionDelayMax: 5000,
     });
     ```

2. **Добавить обработчики событий переподключения:**
   - Добавить обработчик `connect` для восстановления состояния
   - Добавить обработчик `reconnect` для логирования
   - Добавить обработчик `disconnect` для обработки обрыва

3. **Восстановление состояния после переподключения:**
   - При переподключении отправить событие `game:join` снова
   - Обновить состояние игры из ответа сервера

**Пример кода:**
```typescript
// В socket.ts
socket.on('connect', () => {
  console.log('WebSocket connected');
  // Восстановить подключение к игре, если было
  if (currentGameId) {
    socketService.joinGame(currentGameId);
  }
});

socket.on('reconnect', (attemptNumber) => {
  console.log(`WebSocket reconnected after ${attemptNumber} attempts`);
});

socket.on('disconnect', (reason) => {
  console.log('WebSocket disconnected:', reason);
});
```

**Критерии готовности:**
- ✅ WebSocket автоматически переподключается при обрыве
- ✅ Состояние игры восстанавливается после переподключения
- ✅ Пользователь видит индикатор подключения/переподключения

**Файлы для изменения:**
- `frontend/src/services/socket.ts`

---

#### Задача 1.8: Ручное тестирование базовой функциональности

**Цель:** Убедиться, что все основные функции работают.

**Чеклист тестирования:**

1. **Запуск инфраструктуры:**
   - [ ] `docker-compose up -d` - PostgreSQL и Redis запущены
   - [ ] Проверить статус: `docker-compose ps`

2. **Запуск Backend:**
   - [ ] `cd backend && python run.py` - сервер запускается без ошибок
   - [ ] Проверить `/health` - должен вернуть `{"status": "ok"}`
   - [ ] Проверить Swagger UI: `http://localhost:8000/docs`

3. **Запуск Frontend:**
   - [ ] `cd frontend && npm run dev` - приложение запускается
   - [ ] Открыть `http://localhost:5173` - страница загружается

4. **Тестирование регистрации:**
   - [ ] Зарегистрировать нового пользователя
   - [ ] Проверить, что пользователь создан в БД
   - [ ] Проверить, что получен JWT токен

5. **Тестирование логина:**
   - [ ] Войти с правильными данными - должен работать
   - [ ] Войти с неправильным паролем - должна быть ошибка
   - [ ] Проверить, что токен сохраняется в localStorage

6. **Тестирование создания игры:**
   - [ ] Создать игру (авторизованный пользователь)
   - [ ] Проверить, что игра создана в БД
   - [ ] Проверить, что получен invite_code

7. **Тестирование присоединения к игре:**
   - [ ] Открыть второе окно браузера (или инкогнито)
   - [ ] Зарегистрировать второго пользователя
   - [ ] Присоединиться к игре по invite_code
   - [ ] Проверить, что оба пользователя видят игру

8. **Тестирование WebSocket:**
   - [ ] Открыть игру в двух окнах
   - [ ] Переместить токен (мастер)
   - [ ] Проверить, что токен переместился во втором окне
   - [ ] Проверить, что игрок не может перемещать токены

9. **Тестирование загрузки карты:**
   - [ ] Загрузить карту (мастер)
   - [ ] Проверить, что карта отображается
   - [ ] Проверить, что карта видна всем игрокам

**Критерии готовности:**
- ✅ Все пункты чеклиста выполнены
- ✅ Нет критических ошибок в консоли браузера
- ✅ Нет ошибок в логах backend

---

### Фаза 2: Тестирование Backend (после Фазы 1)

#### Задача 2.1: Настройка окружения для тестов

**Цель:** Настроить pytest и создать структуру тестов.

**Шаги выполнения:**

1. **Добавить зависимости в `backend/requirements.txt`:**
   ```
   pytest==7.4.3
   pytest-asyncio==0.21.1
   httpx==0.25.2
   pytest-cov==4.1.0
   pytest-mock==3.12.0
   faker==20.1.0
   ```

2. **Установить зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

3. **Создать структуру папок:**
   ```
   backend/
   └── tests/
       ├── __init__.py
       ├── conftest.py
       ├── test_auth.py
       ├── test_games.py
       ├── test_tokens.py
       ├── test_websocket.py
       └── test_models.py
   ```

4. **Создать `tests/conftest.py` с фикстурами:**
   - Тестовая БД (SQLite в памяти или отдельная PostgreSQL)
   - Тестовый клиент FastAPI
   - Моки для Redis
   - Фикстуры для создания тестовых данных

**Критерии готовности:**
- ✅ Все зависимости установлены
- ✅ Структура папок создана
- ✅ `conftest.py` содержит базовые фикстуры

**Файлы для создания:**
- `backend/tests/__init__.py`
- `backend/tests/conftest.py`
- `backend/requirements.txt` (обновить)

---

#### Задача 2.2: Написание тестов для аутентификации

**Цель:** Покрыть тестами все endpoints аутентификации.

**Тесты для реализации в `tests/test_auth.py`:**

1. **test_register_user_success** - успешная регистрация
2. **test_register_user_duplicate_email** - регистрация с дублирующим email
3. **test_login_success** - успешный вход
4. **test_login_wrong_password** - вход с неправильным паролем
5. **test_login_nonexistent_user** - вход несуществующего пользователя
6. **test_get_current_user_success** - получение текущего пользователя с валидным токеном
7. **test_get_current_user_no_token** - получение без токена (401)
8. **test_get_current_user_invalid_token** - получение с невалидным токеном

**Шаблон теста:**
```python
def test_register_user_success(client, db_session):
    response = client.post(
        "/api/auth/register",
        json={
            "email": "test@example.com",
            "username": "testuser",
            "password": "password123"
        }
    )
    assert response.status_code == 200
    assert "token" in response.json()
    assert "user" in response.json()
```

**Критерии готовности:**
- ✅ Все 8 тестов написаны
- ✅ Все тесты проходят: `pytest tests/test_auth.py -v`
- ✅ Покрытие >80% для `app/api/auth.py`

**Файлы для создания:**
- `backend/tests/test_auth.py`

---

#### Задача 2.3: Написание тестов для игр

**Цель:** Покрыть тестами все endpoints игр.

**Тесты для реализации в `tests/test_games.py`:**

1. **test_create_game_success** - создание игры
2. **test_create_game_unauthorized** - создание без авторизации (401)
3. **test_get_game_by_invite_code** - получение по invite-коду
4. **test_get_game_invalid_invite_code** - несуществующий invite-код (404)
5. **test_join_game_success** - присоединение к игре
6. **test_join_game_nonexistent** - присоединение к несуществующей игре (404)
7. **test_get_game_info** - получение деталей игры
8. **test_generate_unique_invite_code** - генерация уникального invite-кода

**Критерии готовности:**
- ✅ Все 8 тестов написаны
- ✅ Все тесты проходят: `pytest tests/test_games.py -v`
- ✅ Покрытие >80% для `app/api/games.py`

**Файлы для создания:**
- `backend/tests/test_games.py`

---

#### Задача 2.4: Написание тестов для токенов

**Цель:** Покрыть тестами все endpoints токенов.

**Тесты для реализации в `tests/test_tokens.py`:**

1. **test_create_token_master** - создание токена мастером
2. **test_create_token_player** - создание токена игроком (403)
3. **test_update_token_position_master** - обновление позиции мастером
4. **test_update_token_position_player** - обновление позиции игроком (403)
5. **test_delete_token_master** - удаление токена мастером
6. **test_delete_token_player** - удаление токена игроком (403)
7. **test_get_game_tokens** - получение всех токенов игры

**Критерии готовности:**
- ✅ Все 7 тестов написаны
- ✅ Все тесты проходят: `pytest tests/test_tokens.py -v`
- ✅ Покрытие >80% для токенов в `app/api/games.py`

**Файлы для создания:**
- `backend/tests/test_tokens.py`

---

#### Задача 2.5: Написание тестов для WebSocket

**Цель:** Покрыть тестами WebSocket события.

**Тесты для реализации в `tests/test_websocket.py`:**

1. **test_websocket_connect_valid_token** - подключение с валидным токеном
2. **test_websocket_connect_no_token** - подключение без токена (отклонение)
3. **test_websocket_connect_invalid_token** - подключение с невалидным токеном
4. **test_websocket_game_join** - событие game:join
5. **test_websocket_token_move_master** - перемещение токена мастером
6. **test_websocket_token_move_player** - перемещение токена игроком (403)
7. **test_websocket_token_create** - создание токена
8. **test_websocket_token_delete** - удаление токена
9. **test_websocket_broadcast** - broadcast событий другим игрокам
10. **test_websocket_game_state** - отправка game:state при подключении
11. **test_websocket_disconnect** - обработка отключения

**Критерии готовности:**
- ✅ Все 11 тестов написаны
- ✅ Все тесты проходят: `pytest tests/test_websocket.py -v`
- ✅ Покрытие >80% для `app/sockets/game_events.py`

**Файлы для создания:**
- `backend/tests/test_websocket.py`

---

#### Задача 2.6: Написание тестов для моделей

**Цель:** Покрыть тестами модели БД.

**Тесты для реализации в `tests/test_models.py`:**

1. **test_create_user** - создание пользователя
2. **test_user_email_unique** - уникальность email
3. **test_create_game_session** - создание игровой сессии
4. **test_create_game_participant** - создание участника
5. **test_create_token** - создание токена
6. **test_model_relationships** - проверка связей между моделями

**Критерии готовности:**
- ✅ Все 6 тестов написаны
- ✅ Все тесты проходят: `pytest tests/test_models.py -v`
- ✅ Покрытие >80% для всех моделей

**Файлы для создания:**
- `backend/tests/test_models.py`

---

#### Задача 2.7: Проверка покрытия кода

**Цель:** Достичь покрытия кода >80%.

**Шаги выполнения:**

1. **Запустить тесты с покрытием:**
   ```bash
   pytest --cov=app --cov-report=html --cov-report=term tests/
   ```

2. **Проверить отчет:**
   - Открыть `htmlcov/index.html` в браузере
   - Проверить покрытие каждого модуля

3. **Добавить недостающие тесты:**
   - Найти модули с покрытием <80%
   - Написать дополнительные тесты

**Критерии готовности:**
- ✅ Общее покрытие >80%
- ✅ Покрытие критических модулей (auth, games, tokens) >90%
- ✅ Отчет о покрытии сгенерирован

---

### Фаза 3: Тестирование Frontend (после Фазы 2)

#### Задача 3.1: Настройка окружения для тестов Frontend

**Цель:** Настроить Vitest и React Testing Library.

**Шаги выполнения:**

1. **Добавить зависимости в `frontend/package.json`:**
   ```json
   "devDependencies": {
     "vitest": "^1.0.4",
     "@testing-library/react": "^14.1.2",
     "@testing-library/jest-dom": "^6.1.5",
     "@testing-library/user-event": "^14.5.1",
     "jsdom": "^23.0.1",
     "@vitest/ui": "^1.0.4",
     "msw": "^2.0.0"
   }
   ```

2. **Создать `frontend/vitest.config.ts`:**
   ```typescript
   import { defineConfig } from 'vitest/config';
   import react from '@vitejs/plugin-react';
   
   export default defineConfig({
     plugins: [react()],
     test: {
       environment: 'jsdom',
       setupFiles: ['./src/__tests__/setup.ts'],
     },
   });
   ```

3. **Создать `frontend/src/__tests__/setup.ts`:**
   ```typescript
   import '@testing-library/jest-dom';
   ```

**Критерии готовности:**
- ✅ Все зависимости установлены
- ✅ Конфигурация Vitest создана
- ✅ Можно запустить тесты: `npm run test`

**Файлы для создания:**
- `frontend/vitest.config.ts`
- `frontend/src/__tests__/setup.ts`
- `frontend/package.json` (обновить)

---

#### Задача 3.2-3.7: Написание тестов для Frontend

**Структура тестов:**
- `src/__tests__/components/` - тесты компонентов
- `src/__tests__/pages/` - тесты страниц
- `src/__tests__/store/` - тесты store
- `src/__tests__/services/` - тесты сервисов

**Детальные инструкции аналогичны Фазе 2, но для Frontend.**

---

### Фаза 4: Улучшения (после Фазы 3)

Детальные инструкции для каждой задачи улучшения будут добавлены по мере необходимости.

---

## ❌ Что нужно сделать

### Критические задачи (обязательно для работы)

#### 1. Миграции базы данных
- ❌ **Создать первую миграцию Alembic**
  - Команда: `alembic revision --autogenerate -m "Initial migration"`
  - Проверить, что все модели включены
  - Файл должен быть в `backend/alembic/versions/`
  
- ❌ **Применить миграцию**
  - Команда: `alembic upgrade head`
  - Проверить создание таблиц в PostgreSQL

**Приоритет:** КРИТИЧЕСКИЙ

#### 2. Файлы окружения
- ❌ **Создать `backend/.env` из `.env.example`**
  - Установить `SECRET_KEY` (сгенерировать безопасный ключ)
  - Проверить `DATABASE_URL` и `REDIS_URL`
  
- ❌ **Создать `frontend/.env` из `.env.example`**
  - Проверить `VITE_API_URL` и `VITE_WS_URL`

**Приоритет:** КРИТИЧЕСКИЙ

#### 3. Дефолтная карта
- ❌ **Добавить дефолтную карту для тестирования**
  - Создать папку `frontend/public/images/` (если не существует)
  - Создать `frontend/public/images/default-map.jpg` (или использовать placeholder)
  - Можно использовать изображение из `Example/dungeons-and-dragons-website/public/images/dnd-map.jpg`
  - Проверить, что путь `/images/default-map.jpg` корректно работает в браузере
  - Обновить `GameMap.tsx` для использования дефолтной карты (уже настроено: `/images/default-map.jpg`)

**Приоритет:** ВЫСОКИЙ

#### 4. Исправление критических багов в коде
- ⚠️ **Проверить экспорт в `backend/app/main.py`**
  - В конце файла экспортируется `socket_app` вместо `app`
  - Это правильный подход для Socket.IO с FastAPI (ASGIApp оборачивает FastAPI app)
  - Но нужно убедиться, что `run.py` или uvicorn запускают правильный объект
  - Проверить, что `uvicorn app.main:app` работает корректно
  - Если нет - использовать `uvicorn app.main:socket_app` или исправить экспорт
  
- ❌ **Исправить зависимости в `frontend/src/pages/GameRoom.tsx`**
  - В `useEffect` используется `game` в замыкании, но он не в зависимостях
  - Это может привести к устаревшим значениям
  - Решение: добавить `game` в массив зависимостей или использовать `currentGame` из store
  
- ❌ **Добавить обработку переподключения WebSocket**
  - При обрыве связи WebSocket должен автоматически переподключаться
  - Нужно обрабатывать события `disconnect` и `reconnect`
  - Восстанавливать состояние игры после переподключения

**Приоритет:** КРИТИЧЕСКИЙ

---

### Тестирование

#### Backend тесты (Python pytest)

**Требуется создать структуру:**
```
backend/
├── tests/
│   ├── __init__.py
│   ├── conftest.py          # Фикстуры (тестовая БД, клиент)
│   ├── test_auth.py         # Тесты аутентификации
│   ├── test_games.py        # Тесты игровых сессий
│   ├── test_tokens.py       # Тесты токенов
│   ├── test_websocket.py    # Тесты WebSocket
│   └── test_models.py       # Тесты моделей
```

**Зависимости для тестов:**
- `pytest==7.4.3`
- `pytest-asyncio==0.21.1`
- `httpx==0.25.2` (для тестирования FastAPI)
- `pytest-cov==4.1.0` (покрытие кода)
- `pytest-mock==3.12.0` (мокирование)
- `faker==20.1.0` (генерация тестовых данных)
- `testcontainers-postgres==0.5.0` (опционально, для интеграционных тестов)

**Тесты для реализации:**

1. **test_auth.py**
   - ✅ Тест регистрации пользователя
   - ✅ Тест регистрации с дублирующим email (должна быть ошибка)
   - ✅ Тест логина с правильными данными
   - ✅ Тест логина с неправильным паролем
   - ✅ Тест логина с несуществующим email
   - ✅ Тест получения текущего пользователя с валидным токеном
   - ✅ Тест получения текущего пользователя без токена (401)
   - ✅ Тест получения текущего пользователя с невалидным токеном

2. **test_games.py**
   - ✅ Тест создания игры (авторизованный пользователь)
   - ✅ Тест создания игры без авторизации (401)
   - ✅ Тест получения игры по invite-коду
   - ✅ Тест получения игры с несуществующим invite-кодом (404)
   - ✅ Тест присоединения к игре
   - ✅ Тест присоединения к несуществующей игре (404)
   - ✅ Тест получения деталей игры
   - ✅ Тест генерации уникального invite-кода

3. **test_tokens.py**
   - ✅ Тест создания токена мастером
   - ✅ Тест создания токена игроком (403)
   - ✅ Тест обновления позиции токена мастером
   - ✅ Тест обновления позиции токена игроком (403)
   - ✅ Тест удаления токена мастером
   - ✅ Тест удаления токена игроком (403)
   - ✅ Тест получения всех токенов игры

4. **test_websocket.py**
   - ✅ Тест подключения с валидным токеном
   - ✅ Тест подключения без токена (отклонение)
   - ✅ Тест подключения с невалидным токеном (отклонение)
   - ✅ Тест события `game:join`
   - ✅ Тест события `token:move` (только мастер)
   - ✅ Тест события `token:create` (только мастер)
   - ✅ Тест события `token:delete` (только мастер)
   - ✅ Тест broadcast событий другим игрокам
   - ✅ Тест отправки `game:state` при подключении
   - ✅ Тест обработки отключения (disconnect)
   - ✅ Тест очистки комнат при отключении
   - ✅ Тест обработки ошибок в WebSocket событиях

5. **test_models.py**
   - ✅ Тест создания пользователя
   - ✅ Тест уникальности email
   - ✅ Тест создания игровой сессии
   - ✅ Тест создания участника игры
   - ✅ Тест создания токена
   - ✅ Тест связей между моделями

**Приоритет:** ВЫСОКИЙ

#### Frontend тесты (Vitest + React Testing Library)

**Требуется создать структуру:**
```
frontend/
├── src/
│   └── __tests__/
│       ├── components/
│       │   ├── GameMap.test.tsx
│       │   ├── Token.test.tsx
│       │   └── PlayerList.test.tsx
│       ├── pages/
│       │   ├── Login.test.tsx
│       │   ├── Register.test.tsx
│       │   └── MainMenu.test.tsx
│       ├── services/
│       │   ├── api.test.ts
│       │   └── socket.test.ts
│       └── store/
│           ├── authStore.test.ts
│           └── gameStore.test.ts
```

**Зависимости для тестов:**
- `vitest==1.0.4`
- `@testing-library/react==14.1.2`
- `@testing-library/jest-dom==6.1.5`
- `@testing-library/user-event==14.5.1`
- `jsdom==23.0.1`
- `@vitest/ui==1.0.4` (опционально, UI для тестов)
- `msw==2.0.0` (Mock Service Worker для мокирования API)

**Тесты для реализации:**

1. **components/GameMap.test.tsx**
   - ✅ Рендеринг карты
   - ✅ Отображение токенов
   - ✅ Отображение сетки

2. **components/Token.test.tsx**
   - ✅ Рендеринг токена
   - ✅ Отображение имени
   - ✅ Drag & Drop (только для мастера)
   - ✅ Блокировка перемещения для игрока

3. **pages/Login.test.tsx**
   - ✅ Рендеринг формы
   - ✅ Валидация полей
   - ✅ Успешный логин
   - ✅ Обработка ошибок

4. **pages/Register.test.tsx**
   - ✅ Рендеринг формы
   - ✅ Валидация полей
   - ✅ Успешная регистрация
   - ✅ Обработка ошибок

5. **store/authStore.test.ts**
   - ✅ Логин
   - ✅ Регистрация
   - ✅ Выход
   - ✅ Проверка авторизации

6. **store/gameStore.test.ts**
   - ✅ Установка игры
   - ✅ Обновление токенов
   - ✅ Обновление позиции токена
   - ✅ Добавление/удаление токенов
   - ✅ Сброс состояния (reset)

7. **services/api.test.ts**
   - ✅ Тест успешных запросов
   - ✅ Тест обработки ошибок
   - ✅ Тест добавления токена в заголовки
   - ✅ Тест редиректа при 401

8. **services/socket.test.ts**
   - ✅ Тест подключения
   - ✅ Тест отключения
   - ✅ Тест отправки событий
   - ✅ Тест подписки на события

**Приоритет:** СРЕДНИЙ

---

### Улучшения функциональности

#### 1. Обработка ошибок
- ❌ **Улучшить обработку ошибок на фронтенде**
  - Показывать понятные сообщения об ошибках
  - Обработка сетевых ошибок (нет интернета, таймауты)
  - Обработка ошибок WebSocket (переподключение)
  - Централизованный error boundary для React
  - Обработка ошибок загрузки изображений карт
  
- ❌ **Улучшить обработку ошибок на бэкенде**
  - Единый формат ошибок (создать `app/utils/errors.py`)
  - Логирование ошибок (настроить структурированное логирование)
  - Глобальный обработчик исключений FastAPI
  - Валидация входных данных в Pydantic схемах
  - Обработка ошибок БД (connection errors, transaction errors)
  - Обработка ошибок Redis (connection errors)

**Приоритет:** ВЫСОКИЙ

#### 2. Валидация данных
- ❌ **Добавить валидацию на фронтенде**
  - Использовать react-hook-form + zod (уже в зависимостях)
  - Валидация email, пароля, username
  - Валидация invite-кода
  
- ❌ **Улучшить валидацию на бэкенде**
  - Дополнительные проверки в Pydantic схемах
  - Валидация позиций токенов (0-100%)

**Приоритет:** СРЕДНИЙ

#### 3. UI/UX улучшения
- ❌ **Добавить индикатор загрузки**
  - При создании игры
  - При подключении к игре
  - При загрузке данных
  - При перемещении токенов (опционально)
  
- ❌ **Добавить уведомления (toast)**
  - Успешные операции (создание игры, присоединение)
  - Ошибки (понятные сообщения)
  - Можно использовать библиотеку `sonner` или `react-toastify`
  
- ❌ **Улучшить визуальное отображение токенов**
  - Добавить иконки/аватары (использовать image_url из токена)
  - Добавить цвета для разных персонажей
  - Улучшить hover эффекты
  - Добавить визуальную индикацию при перетаскивании
  
- ❌ **Добавить UI для управления токенами**
  - Кнопка "Создать токен" (только для мастера)
  - Контекстное меню на токене (удалить, переименовать)
  - Форма создания токена с выбором позиции
  
- ❌ **Добавить UI для загрузки карт**
  - Кнопка "Загрузить карту" (только для мастера)
  - Форма загрузки с предпросмотром
  - Индикатор прогресса загрузки
  
- ❌ **Добавить адаптивный дизайн**
  - Поддержка мобильных устройств
  - Адаптация под разные размеры экрана
  - Оптимизация для планшетов

**Приоритет:** СРЕДНИЙ

#### 4. Безопасность
- ❌ **Добавить rate limiting**
  - Ограничение количества запросов
  - Защита от брутфорса на логине
  
- ❌ **Улучшить безопасность паролей**
  - Минимальная длина пароля
  - Требования к сложности пароля
  
- ❌ **Добавить CSRF защиту**
  - Для критических операций
  
- ❌ **Валидация файлов загрузки**
  - Проверка MIME типа
  - Проверка размера файла
  - Сканирование на вирусы (опционально)

**Приоритет:** ВЫСОКИЙ

#### 5. Производительность
- ❌ **Оптимизация WebSocket**
  - Дебаунс для частых событий перемещения (на фронтенде)
  - Батчинг обновлений токенов
  - Оптимистичные обновления UI
  
- ❌ **Кеширование**
  - Кеширование данных игр в Redis (уже частично реализовано)
  - Кеширование статических файлов (карты)
  - Кеширование метаданных игр
  
- ❌ **Оптимизация запросов**
  - Пагинация для списков (если понадобится)
  - Ленивая загрузка данных
  - Оптимизация SQL запросов (eager loading для связей)

**Приоритет:** НИЗКИЙ

#### 6. Логирование и мониторинг
- ❌ **Настроить логирование на бэкенде**
  - Использовать `structlog` или `loguru` для структурированного логирования
  - Логировать все важные события (регистрация, создание игры, перемещение токенов)
  - Логировать ошибки с полным контекстом
  - Настроить уровни логирования (DEBUG, INFO, WARNING, ERROR)
  
- ❌ **Добавить мониторинг**
  - Health check endpoint (уже есть `/health`, но можно улучшить)
  - Метрики производительности (время ответа API, WebSocket задержка)
  - Мониторинг использования ресурсов (память, CPU)
  - Алерты при критических ошибках

**Приоритет:** СРЕДНИЙ

#### 7. Интеграционное тестирование
- ❌ **E2E тесты для основных сценариев**
  - Регистрация → Создание игры → Присоединение второго игрока → Перемещение токенов
  - Проверка синхронизации между несколькими клиентами
  - Использовать Playwright или Cypress
  
- ❌ **Нагрузочное тестирование**
  - Тест с 10+ одновременными подключениями
  - Тест частых перемещений токенов
  - Измерение задержек и использования ресурсов

**Приоритет:** СРЕДНИЙ

#### 8. Линтинг и форматирование кода
- ❌ **Настроить линтинг для Python**
  - Добавить `ruff` или `flake8` + `black` для форматирования
  - Настроить `mypy` для проверки типов
  - Добавить pre-commit hooks
  
- ❌ **Настроить линтинг для TypeScript**
  - Настроить ESLint (уже в зависимостях, но не настроен)
  - Настроить Prettier для форматирования
  - Добавить pre-commit hooks

**Приоритет:** СРЕДНИЙ

#### 9. Проверка совместимости
- ❌ **Тестирование в разных браузерах**
  - Chrome/Chromium
  - Firefox
  - Safari (если возможно)
  - Edge
  
- ❌ **Проверка работы на разных устройствах**
  - Десктоп (основной)
  - Планшет (опционально)
  - Мобильный (опционально, может потребовать адаптации UI)

**Приоритет:** СРЕДНИЙ

#### 10. Документация API
- ❌ **Улучшить Swagger документацию**
  - FastAPI автоматически генерирует Swagger, но можно улучшить описания
  - Добавить примеры запросов/ответов
  - Добавить описания ошибок
  
- ❌ **Создать Postman коллекцию**
  - Экспорт всех API endpoints
  - Примеры запросов
  - Переменные окружения

**Приоритет:** НИЗКИЙ

#### 11. CI/CD (опционально)
- ❌ **Настроить GitHub Actions или аналоги**
  - Автоматический запуск тестов при push
  - Проверка линтинга и форматирования
  - Автоматический деплой (опционально)

**Приоритет:** НИЗКИЙ

---

### Дополнительные функции (не для MVP)

#### 1. Листы персонажей
- ❌ Создание и редактирование персонажей
- ❌ Привязка токенов к персонажам
- ❌ Отображение характеристик при клике на токен

#### 2. Чат
- ❌ Текстовый чат в игре
- ❌ Системные сообщения
- ❌ История сообщений

#### 3. Броски костей
- ❌ UI для выбора костей
- ❌ Отправка броска на сервер
- ❌ Отображение результата всем игрокам

#### 4. Fog of War
- ❌ Скрытие областей карты
- ❌ Инструменты для раскрытия областей
- ❌ Разные уровни видимости

#### 5. Загрузка и выбор карт
- ❌ Галерея карт
- ❌ Выбор карты при создании игры
- ❌ Предпросмотр карт

---

## 📝 Порядок выполнения задач

> **ВАЖНО ДЛЯ НЕЙРОСЕТИ:** Начните с раздела [Пошаговый план реализации](#-пошаговый-план-реализации). Там содержатся детальные инструкции для каждой задачи с указанием файлов, строк кода и ожидаемых результатов.

### Фаза 1: Критические исправления (1-2 дня)

**См. детальные инструкции в разделе [Пошаговый план реализации - Фаза 1](#фаза-1-критические-исправления-обязательно-первым)**

1. ✅ [Задача 1.1](#задача-11-создание-миграций-базы-данных) - Создать и применить миграции БД
2. ✅ [Задача 1.2](#задача-12-создание-файлов-окружения) - Создать .env файлы (backend и frontend)
3. ✅ [Задача 1.3](#задача-13-исправление-бага-в-mainpy-экспорт-socket_app) - Исправить экспорт в `main.py`
4. ✅ [Задача 1.4](#задача-14-исправление-бага-в-gameroomtsx-зависимости-useeffect) - Исправить зависимости в `GameRoom.tsx`
5. ✅ [Задача 1.5](#задача-15-исправление-бага-в-mapspy-game_id-как-query-параметр) - Исправить `game_id` в `maps.py`
6. ✅ [Задача 1.6](#задача-16-добавление-дефолтной-карты) - Добавить дефолтную карту
7. ✅ [Задача 1.7](#задача-17-добавление-обработки-переподключения-websocket) - Добавить обработку переподключения WebSocket
8. ✅ [Задача 1.8](#задача-18-ручное-тестирование-базовой-функциональности) - Протестировать базовую функциональность вручную

### Фаза 2: Тестирование Backend (2-3 дня)

**См. детальные инструкции в разделе [Пошаговый план реализации - Фаза 2](#фаза-2-тестирование-backend-после-фазы-1)**

1. ✅ [Задача 2.1](#задача-21-настройка-окружения-для-тестов) - Настроить pytest окружение
2. ✅ [Задача 2.2](#задача-22-написание-тестов-для-аутентификации) - Написать тесты для аутентификации
3. ✅ [Задача 2.3](#задача-23-написание-тестов-для-игр) - Написать тесты для игр
4. ✅ [Задача 2.4](#задача-24-написание-тестов-для-токенов) - Написать тесты для токенов
5. ✅ [Задача 2.5](#задача-25-написание-тестов-для-websocket) - Написать тесты для WebSocket
6. ✅ [Задача 2.6](#задача-26-написание-тестов-для-моделей) - Написать тесты для моделей
7. ✅ [Задача 2.7](#задача-27-проверка-покрытия-кода) - Достичь покрытия кода >80%

### Фаза 3: Тестирование Frontend (2-3 дня)

**См. детальные инструкции в разделе [Пошаговый план реализации - Фаза 3](#фаза-3-тестирование-frontend-после-фазы-2)**

1. ✅ [Задача 3.1](#задача-31-настройка-окружения-для-тестов-frontend) - Настроить Vitest окружение
2. ✅ Написать тесты для компонентов
3. ✅ Написать тесты для страниц
4. ✅ Написать тесты для store
5. ✅ Написать тесты для сервисов
6. ✅ Настроить покрытие кода

### Фаза 4: Улучшения (3-5 дней)

**См. раздел [Дополнительные улучшения](#дополнительные-улучшения) для деталей**

1. ❌ Улучшить обработку ошибок
2. ❌ Добавить валидацию данных
3. ❌ Улучшить UI/UX
4. ❌ Добавить безопасность
5. ❌ Оптимизировать производительность
6. ❌ Настроить логирование и мониторинг
7. ❌ Настроить линтинг и форматирование кода

### Фаза 5: Дополнительные функции (по необходимости)
1. ❌ Листы персонажей
2. ❌ Чат
3. ❌ Броски костей
4. ❌ Fog of War
5. ❌ Загрузка и выбор карт
6. ❌ История действий (undo/redo)

---

## 🔧 Технические детали для продолжения разработки

### Структура базы данных

После применения миграций будут созданы таблицы:

```sql
-- Пользователи
CREATE TABLE users (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    email VARCHAR(255) UNIQUE NOT NULL,
    username VARCHAR(100) NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- Игровые сессии
CREATE TABLE game_sessions (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    name VARCHAR(255) NOT NULL,
    invite_code VARCHAR(6) UNIQUE NOT NULL,
    master_id UUID NOT NULL REFERENCES users(id),
    map_url VARCHAR(500),
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- Участники игр
CREATE TABLE game_participants (
    game_id UUID NOT NULL REFERENCES game_sessions(id) ON DELETE CASCADE,
    user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    role VARCHAR(20) NOT NULL DEFAULT 'player',
    joined_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
    PRIMARY KEY (game_id, user_id)
);

-- Токены на карте
CREATE TABLE tokens (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    game_id UUID NOT NULL REFERENCES game_sessions(id) ON DELETE CASCADE,
    name VARCHAR(255) NOT NULL,
    x FLOAT NOT NULL,
    y FLOAT NOT NULL,
    image_url VARCHAR(500),
    character_id UUID,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- Индексы
CREATE INDEX idx_users_email ON users(email);
CREATE INDEX idx_game_sessions_invite_code ON game_sessions(invite_code);
CREATE INDEX idx_tokens_game_id ON tokens(game_id);
```

### API Endpoints (полный список)

**Аутентификация:**
- `POST /api/auth/register` - регистрация
- `POST /api/auth/login` - вход
- `GET /api/auth/me` - текущий пользователь

**Игры:**
- `POST /api/games` - создать лобби
- `GET /api/games/{invite_code}` - информация о лобби
- `POST /api/games/{game_id}/join` - присоединиться
- `GET /api/games/{game_id}/info` - детали игры
- `GET /api/games/{game_id}/tokens` - получить все токены
- `POST /api/games/{game_id}/tokens` - создать токен
- `PUT /api/games/{game_id}/tokens/{token_id}` - обновить позицию
- `DELETE /api/games/{game_id}/tokens/{token_id}` - удалить токен

**Карты:**
- `POST /api/maps/upload?game_id={game_id}` - загрузить карту (только мастер)
  - ⚠️ **ТРЕБУЕТСЯ ИСПРАВЛЕНИЕ**: `game_id` должен быть Query параметром
  - Принимает multipart/form-data с файлом
  - Валидация размера (макс 10MB) и типа файла
  - Сохранение в `uploads/maps/` с UUID именем

### WebSocket события

**Клиент → Сервер:**
- `game:join` - `{game_id: string}`
- `token:move` - `{game_id: string, token_id: string, x: number, y: number}`
- `token:create` - `{game_id: string, name: string, x: number, y: number, image_url?: string}`
- `token:delete` - `{game_id: string, token_id: string}`

**Сервер → Клиент:**
- `game:state` - полное состояние игры
- `token:moved` - токен перемещен
- `token:created` - токен создан
- `token:deleted` - токен удален
- `player:joined` - игрок подключился
- `player:left` - игрок отключился
- `error` - ошибка

### Переменные окружения

**Backend (.env):**
```env
# Database
DATABASE_URL=postgresql://dnd_user:dnd_password@localhost:5432/dnd_db

# Redis
REDIS_URL=redis://localhost:6379

# JWT
SECRET_KEY=your-secret-key-here-change-in-production-use-secrets-token-urlsafe-base64
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Server
HOST=0.0.0.0
PORT=8000

# Uploads
UPLOAD_DIR=uploads
MAPS_DIR=uploads/maps
MAX_UPLOAD_SIZE=10485760

# Logging (опционально)
LOG_LEVEL=INFO
LOG_FORMAT=json
```

**Frontend (.env):**
```env
VITE_API_URL=http://localhost:8000
VITE_WS_URL=http://localhost:8000

# Опционально для разработки
VITE_ENABLE_DEV_TOOLS=true
```

---

## 📊 Метрики успеха

### Функциональность
- ✅ Регистрация и вход работают
- ✅ Создание и присоединение к игре работает
- ✅ Перетаскивание токенов работает
- ✅ Синхронизация в реальном времени работает

### Качество кода
- ⚠️ Покрытие тестами: 0% (цель: >80%)
- ⚠️ Линтинг: не настроен (ESLint для TS, ruff/flake8 для Python)
- ⚠️ Форматирование: не настроено (Prettier для TS, black для Python)
- ⚠️ Проверка типов: не настроена (mypy для Python, tsc для TypeScript)
- ⚠️ Pre-commit hooks: не настроены

### Производительность
- ⚠️ Время ответа API: не измерено (цель: <200ms для 95 перцентиля)
- ⚠️ Задержка WebSocket: не измерена (цель: <100ms для broadcast)
- ⚠️ Время загрузки страницы: не измерено (цель: <3s для First Contentful Paint)
- ⚠️ Размер бандла фронтенда: не измерен (цель: <500KB gzipped)
- ⚠️ Использование памяти: не измерено
- ⚠️ Нагрузочное тестирование: не проведено (цель: поддержка 50+ одновременных пользователей)

---

## 🔍 Известные проблемы и ограничения

### Критические проблемы
1. **Экспорт в main.py**: В конце файла экспортируется `socket_app` вместо `app` - нужно проверить корректность работы при запуске через uvicorn
2. **Зависимости в GameRoom.tsx**: В `useEffect` используется `game` в замыкании без зависимости (строка 55), что может привести к устаревшим значениям
3. **game_id в maps.py**: Параметр `game_id` в `upload_map` должен быть Query параметром, а не обычным параметром функции
4. **Нет переподключения WebSocket**: При обрыве связи нет автоматического переподключения - Socket.IO имеет встроенную поддержку, но нужно проверить настройки
5. **Нет валидации на фронтенде**: Формы не используют react-hook-form + zod (хотя библиотеки есть в зависимостях)
6. **Нет обработки ошибок загрузки карт**: Если файл не читается или путь неверный, нет понятной ошибки пользователю
7. **Нет проверки существования папок**: При первом запуске могут быть проблемы, если папки не созданы (хотя есть `os.makedirs`, но нужно проверить)
8. **Нет обработки ошибок при сохранении файла**: Если нет прав на запись или диск переполнен, нет обработки

### Потенциальные проблемы
1. **Нет логирования**: Сложно отлаживать проблемы в продакшене
2. **Нет rate limiting**: Возможны атаки на API (брутфорс на логин, спам запросов)
3. **Слабая валидация паролей**: Нет требований к сложности (минимум длина, символы)
4. **Нет проверки размера файлов на клиенте**: Пользователь может попытаться загрузить огромный файл
5. **Нет обработки таймаутов**: Долгие запросы могут висеть без ответа
6. **Нет обработки конфликтов**: При одновременном перемещении токена могут быть конфликты
7. **Нет очистки неактивных игр**: Игры могут накапливаться в БД
8. **Нет ограничения на количество участников**: Игра может иметь неограниченное количество игроков

### Ограничения MVP
1. **Нет листов персонажей**: Токены не привязаны к персонажам, нет характеристик
2. **Нет чата**: Нет возможности общаться в игре
3. **Нет бросков костей**: Нет игровой механики D&D
4. **Нет Fog of War**: Все видят всю карту, нет скрытых областей
5. **Нет истории действий**: Нет возможности откатить изменения (undo/redo)
6. **Нет сохранения состояния в Redis**: При перезапуске сервера состояние в Redis теряется (данные в БД сохраняются)
7. **Нет масштабирования карты**: Нет zoom in/out, только фиксированный размер
8. **Нет панорамирования**: Карта не двигается, только токены
9. **Нет инструментов мастера**: Нет дополнительных инструментов для DM (измерение расстояний, зоны эффектов)
10. **Нет управления игроками**: Мастер не может исключать игроков, менять роли
11. **Нет создания токенов через UI**: Токены можно создавать только через API/WebSocket (нет UI кнопки)
12. **Нет удаления токенов через UI**: Токены можно удалять только через API/WebSocket (нет UI кнопки)
13. **Нет загрузки карт через UI**: Загрузка карт только через API (нет UI формы)

---

## 🎯 Заключение

**Текущий статус:** MVP прототип реализован и готов к тестированию. Основная функциональность работает, но требуется:
1. Создание и применение миграций БД
2. Настройка окружения (.env файлы)
3. Исправление критических багов в коде
4. Написание тестов (критично)
5. Улучшение обработки ошибок и валидации
6. UI/UX улучшения
7. Настройка логирования и мониторинга

**Следующие шаги:**
1. Выполнить Фазу 1 (критические исправления) - **ОБЯЗАТЕЛЬНО ПЕРВЫМ**
2. Написать тесты (Фаза 2 и 3) - **КРИТИЧНО**
3. Улучшить качество кода (Фаза 4) - **ВАЖНО**

**Оценка времени до готового прототипа:**
- Фаза 1: 1-2 дня (критические исправления)
- Фаза 2: 2-3 дня (backend тесты)
- Фаза 3: 2-3 дня (frontend тесты)
- Фаза 4: 3-5 дней (улучшения)
- **Итого: 8-13 дней** до полностью рабочего и протестированного прототипа

**Минимальный MVP (только критическое):**
- Фаза 1: 1-2 дня
- Базовое тестирование: 1 день
- **Итого: 2-3 дня** до работающего прототипа для демонстрации

После выполнения этих задач прототип будет готов к демонстрации и дальнейшему развитию.

---

## 📚 Дополнительные ресурсы

### Полезные ссылки
- [FastAPI документация](https://fastapi.tiangolo.com/)
- [Socket.IO документация](https://socket.io/docs/v4/)
- [React документация](https://react.dev/)
- [SQLAlchemy документация](https://docs.sqlalchemy.org/)
- [Alembic документация](https://alembic.sqlalchemy.org/)
- [Pytest документация](https://docs.pytest.org/)
- [Vitest документация](https://vitest.dev/)

### Рекомендуемые инструменты для разработки
- **Postman/Insomnia** - для тестирования API
- **pgAdmin/DBeaver** - для работы с PostgreSQL
- **Redis Commander/RedisInsight** - для просмотра данных в Redis
- **Browser DevTools** - для отладки фронтенда и WebSocket
- **VS Code с расширениями** - Python, TypeScript, ESLint, Prettier
- **Docker Desktop** - для управления контейнерами
- **Git** - для контроля версий

### Команды для быстрого старта

**Backend:**
```bash
cd backend
python -m venv venv
source venv/bin/activate  # или venv\Scripts\activate на Windows
pip install -r requirements.txt
alembic revision --autogenerate -m "Initial migration"
alembic upgrade head
python run.py
```

**Frontend:**
```bash
cd frontend
npm install
npm run dev
```

**Базы данных:**
```bash
docker-compose up -d
docker-compose ps  # проверить статус
docker-compose logs -f  # просмотр логов
```

### Чеклист перед первым запуском

**Обязательно:**
- [ ] Docker и Docker Compose установлены и работают
- [ ] Python 3.11+ установлен
- [ ] Node.js 18+ установлен
- [ ] PostgreSQL и Redis запущены через docker-compose
- [ ] Создан `backend/.env` с правильными значениями
- [ ] Создан `frontend/.env` с правильными значениями
- [ ] Сгенерирован безопасный SECRET_KEY
- [ ] Применены миграции БД (`alembic upgrade head`)
- [ ] Создана папка `frontend/public/images/` с дефолтной картой

**Рекомендуется:**
- [ ] Проверена работа API через Swagger UI (`http://localhost:8000/docs`)
- [ ] Проверена работа WebSocket через DevTools
- [ ] Протестирована регистрация и логин
- [ ] Протестировано создание игры
- [ ] Протестировано подключение нескольких клиентов

---

## 🚀 Деплой (для будущего)

### Варианты деплоя

1. **Простой деплой (VPS)**
   - Установить Docker на VPS
   - Скопировать проект
   - Настроить .env файлы
   - Запустить через docker-compose
   - Настроить Nginx как reverse proxy
   - Настроить SSL сертификат (Let's Encrypt)

2. **Облачный деплой**
   - **Backend**: Railway, Render, Fly.io, или AWS/GCP
   - **Frontend**: Vercel, Netlify, или статический хостинг
   - **PostgreSQL**: Managed PostgreSQL (AWS RDS, Supabase, Neon)
   - **Redis**: Managed Redis (Upstash, Redis Cloud)

3. **Docker деплой**
   - Создать Dockerfile для backend
   - Создать Dockerfile для frontend
   - Использовать docker-compose для production
   - Настроить volumes для данных

### Требования для production

- [ ] Настроить переменные окружения для production
- [ ] Использовать сильный SECRET_KEY
- [ ] Настроить CORS для production домена
- [ ] Настроить SSL/TLS
- [ ] Настроить резервное копирование БД
- [ ] Настроить мониторинг и алерты
- [ ] Настроить логирование в файлы или внешний сервис
- [ ] Настроить rate limiting
- [ ] Оптимизировать производительность

---

## 📌 Краткое резюме для быстрого старта

### Что работает прямо сейчас:
✅ Backend API полностью реализован  
✅ Frontend UI полностью реализован  
✅ WebSocket синхронизация реализована  
✅ Базовая функциональность готова к тестированию  

### Что нужно сделать СЕЙЧАС (критично):

**👉 НАЧНИТЕ С РАЗДЕЛА [Пошаговый план реализации - Фаза 1](#фаза-1-критические-исправления-обязательно-первым)**

1. ✅ [Задача 1.1](#задача-11-создание-миграций-базы-данных) - Создать миграции БД и применить их
2. ✅ [Задача 1.2](#задача-12-создание-файлов-окружения) - Создать .env файлы
3. ✅ [Задача 1.3](#задача-13-исправление-бага-в-mainpy-экспорт-socket_app) - Исправить баг в main.py
4. ✅ [Задача 1.4](#задача-14-исправление-бага-в-gameroomtsx-зависимости-useeffect) - Исправить баг в GameRoom.tsx
5. ✅ [Задача 1.5](#задача-15-исправление-бага-в-mapspy-game_id-как-query-параметр) - Исправить баг в maps.py
6. ✅ [Задача 1.6](#задача-16-добавление-дефолтной-карты) - Добавить дефолтную карту
7. ✅ [Задача 1.7](#задача-17-добавление-обработки-переподключения-websocket) - Добавить переподключение WebSocket
8. ✅ [Задача 1.8](#задача-18-ручное-тестирование-базовой-функциональности) - Протестировать вручную

### Что нужно сделать ДАЛЬШЕ (важно):

**👉 ПЕРЕЙДИТЕ К РАЗДЕЛУ [Пошаговый план реализации - Фаза 2](#фаза-2-тестирование-backend-после-фазы-1)**

1. Написать тесты (backend и frontend)
2. Улучшить обработку ошибок
3. Добавить валидацию
4. Улучшить UI/UX

### Оценка времени:
- **Минимальный MVP (работающий прототип)**: 2-3 дня (Фаза 1)
- **Полностью протестированный прототип**: 8-13 дней (Фазы 1-3)

---

## 🎯 Инструкция для нейросети

**При получении задачи на реализацию:**

1. **Найдите соответствующую задачу** в разделе [Пошаговый план реализации](#-пошаговый-план-реализации)
2. **Следуйте детальным инструкциям** - каждая задача содержит:
   - Цель задачи
   - Пошаговые инструкции
   - Конкретные файлы и строки кода
   - Примеры кода (где необходимо)
   - Критерии готовности
3. **Проверьте критерии готовности** после выполнения
4. **Переходите к следующей задаче** только после завершения текущей

**Если задача не найдена в плане:**
- Проверьте раздел [Что нужно сделать](#-что-нужно-сделать)
- Проверьте раздел [Дополнительные улучшения](#дополнительные-улучшения)
- Создайте детальный план по аналогии с существующими задачами

---

**План готов к использованию!** 🎯

